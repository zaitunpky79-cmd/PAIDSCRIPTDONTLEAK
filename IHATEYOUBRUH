--deobfuscated by @Watup-Exploitz | discord.gg/EgbzrBqur2

local AllowedID = 10936706695
local Players = game:GetService("Players")
local LP = Players.LocalPlayer

local function SafetyCheck()
    if LP.UserId ~= AllowedID then
        LP:Kick("DYTON SECURITY: User Not Whitelisted. Access Revoked.")
        return false
    end
    return true
end

if not SafetyCheck() then return end

local servicesToVerify = {"MLModelDeliveryService", "PartyEmulatorService", "OmniRecommendationsService", "PlatformFriendsService"}
for _, serviceName in ipairs(servicesToVerify) do
    local success = pcall(function()
        local service = game:GetService(serviceName)
        if typeof(service) ~= "Instance" or not service:IsA(serviceName) or service.ClassName ~= serviceName or service.Parent ~= game then 
            error("detected by Dyton", 0) 
        end
        local secondaryCheck = game:GetService(serviceName)
        if service ~= secondaryCheck then error("detected by Dyton", 0) end
    end)
    if not success then error("detected by Dyton", 0) end
end

local friendCheck = pcall(function()
    local pf = game:GetService("PlatformFriendsService")
    local fs = game:GetService("FriendService")
    if pf == fs then error("detected by Dyton", 0) end
end)
if not friendCheck then error("detected by Dyton", 0) end

local integrityState = true
local secureKey = "ULTRA_SECURITY_VALIDATION"

local function verifyEnvironment()
    local success, res = pcall(function()
        local t = {}
        table.freeze(t)
        t.newKey = 1
    end)
    if success then return false end

    local success2, res2 = pcall(function()
        return string.dump(function() end)
    end)
    if success2 then return false end

    local timeCheck = pcall(function()
        local t1 = os.time()
        local t2 = os.time()
        if t2 < t1 then integrityState = false end
    end)
    if not timeCheck then return false end

    local tableManipulation = pcall(function()
        local frozen = table.freeze({A = 1})
        local _, err = pcall(function() frozen.A = 2 end)
        if not err then integrityState = false end
    end)
    if not tableManipulation then return false end

    return true
end

local function performAdvancedStaticChecks()
    if not verifyEnvironment() then return false end
    local isDeepStackValid = true
    local _, stackErr = pcall(function()
        local function deepCheck(depth)
            if depth > 0 then deepCheck(depth - 1) else error("deep_marker") end
        end
        deepCheck(10)
    end)
    if not stackErr or not string.find(stackErr, "deep_marker") then isDeepStackValid = false end
    if not isDeepStackValid then return false end

    if type(delay) ~= "function" or type(tick) ~= "function" then return false end
    if math.floor(math.pi) ~= 3 or math.clamp(5, 1, 3) ~= 3 then return false end

    return true
end

local function runAdvancedRealTimeMonitor()
    while integrityState do
        if not verifyEnvironment() then integrityState = false end
        
        local closureCheck = pcall(function()
            local f = function() end
            if getfenv(f) ~= getfenv(0) then integrityState = false end
        end)
        if not closureCheck then integrityState = false end

        local stringAdvancedCheck = pcall(function()
            if string.reverse("ABC") ~= "CBA" or string.split("A,B", ",")[1] ~= "A" then integrityState = false end
        end)
        if not stringAdvancedCheck then integrityState = false end

        local tableFindCheck = pcall(function()
            local array = {"X", "Y"}
            if table.find(array, "X") ~= 1 or table.find(array, "Z") ~= nil then integrityState = false end
        end)
        if not tableFindCheck then integrityState = false end

        if not integrityState then
            while true do
                pcall(function() return coroutine.yield() end)
            end
        end
        task.wait(0.2)
    end
end

local isExecutionSafe, verificationResult = pcall(performAdvancedStaticChecks)
if not isExecutionSafe or verificationResult ~= true then
    integrityState = false
    while true do
        pcall(function() return coroutine.yield() end)
    end
else
    task.spawn(runAdvancedRealTimeMonitor)
end

-- [[ B2 SPIRIT STEALTH BOMBER - RED ABYSS EDITION (CINEMATIC KINETIC OPTIMIZED V3.4) ]] --
-- Engine Core: DYTON_txt (Engine Reset), ruru (Fly Logic Fix), Abyss Mod (Invis)
-- Optimization & Math Core: Josep Bart (Cinematic Kinetic Smoothing & Permanent Prop Lock)

local Players = game:GetService("Players")
local LP = Players.LocalPlayer
local RS = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

local OwnerName = LP.Name
local petMode = false
local flying = false
local noclip = false
local IsInvisible = false

local flySpeed = 1
local vVect = 0
local wingRemoteCache = {}
local PERMANENT_PROP_LOCK = {} -- [[ LOCK SYSTEM DATABASE BY JOSEP BART ]]
local currentCFrames = {}
local lastNetworkUpdate = 0
local lastColorUpdate = 0
local colorIndex = 1

-- [[ ADAPTIVE VARIABLE CONTROL ]] --
local estimatedPing = 0.03   
local lastPosition = Vector3.zero
local smoothedMouseX = 0 

-- [[ MANUVER CONFIGURATION (CINEMATIC SMOOTHING) ]] --
local currentRoll = 0
local MAX_ROLL = 32          
local ROLL_SPEED = 8.5       
local BASE_RETURN_SPEED = 18 

-- =============================================================================
-- [[ CONFIGURASI MANUAL ROTASI ]] --
-- =============================================================================
local MANUAL_ROTATION = Vector3.new(0, 260, 0) 
-- =============================================================================

local COLORS = {
    AbyssRed = Color3.fromRGB(100, 0, 180),   
    DarkAbyss = Color3.fromRGB(30, 0, 60), 
    Void = Color3.fromRGB(10, 0, 20),
    Accent = Color3.fromRGB(190, 50, 255)
}

local function getAbyssColor(t, offset)
    local cycle = (t * 0.3 + (offset * 0.1)) % 1
    return cycle < 0.5 and COLORS.AbyssRed:Lerp(COLORS.DarkAbyss, cycle * 2) or COLORS.DarkAbyss:Lerp(COLORS.Void, (cycle - 0.5) * 2)
end

local function getOff(base, target) return base:ToObjectSpace(target) end
local baseRef = CFrame.new(594.9064331054688, 2.4861183166503906, 1190.9410400390625)

local B2SpiritParts = {
    getOff(baseRef, CFrame.new(594.9064331054688, 2.4861183166503906, 1190.9410400390625, 0.28231799602508545, 0.9581945538520813, -0.046543560922145844, 0.2660927176475525, -0.0316011905670166, 0.963429868221283, 0.9216790199279785, -0.28437942266464233, -0.26388588547706604)),
    getOff(baseRef, CFrame.new(595.5532836914062, 2.5107550621032715, 1194.15185546875, -0.012469688430428505, 0.9989699721336365, -0.04373187571763992, 0.2523232698440552, -0.03918246179819107, -0.9668543934822083, -0.9675675630569458, -0.023088466376066208, -0.25157374143600464)),
    getOff(baseRef, CFrame.new(588.9024047851562, 1.4118287563323975, 1196.1141357421875, -0.7898862957954407, 0.5627877116203308, 0.2435988187789917, -0.2972806990146637, -0.003969630226492882, -0.9547796845436096, -0.536371648311615, -0.8265865445137024, 0.17044596374034882)),
    getOff(baseRef, CFrame.new(588.1817626953125, 1.6392762660980225, 1191.7083740234375, 0.5361765623092651, 0.8276496529579163, 0.16588415205478668, 0.2968396246433258, -0.0008944745641201735, -0.9549252986907959, -0.7901912331581116, 0.561245858669281, -0.24615296721458435)),
    getOff(baseRef, CFrame.new(593.9909057617188, 0.5950697064399719, 1195.1761474609375, 0.5612367987632751, 0.8276577591896057, -0.0007732717203907669, 0.0000041778735067055095, -0.0009412630461156368, -0.9999995827674866, -0.8276553153991699, 0.5612379908561707, -0.0005325737874954939)),
    getOff(baseRef, CFrame.new(588.9212646484375, 0.6666668057441711, 1196.454345703125, -0.8266053795814514, 0.5627843141555786, -0.002305620815604925, 0.000012448386769392528, -0.004078791011124849, -0.9999945759773254, -0.5627894401550293, -0.8265957236289978, 0.0033611005637794733)),
    getOff(baseRef, CFrame.new(597.4761962890625, 0.6203178763389587, 1184.063720703125, -0.8266026973724365, 0.562784731388092, -0.0023054538760334253, 0.000015136844922380988, -0.004074392840266228, -0.9999931454658508, -0.5627897381782532, -0.8265944123268127, 0.0033576630521565676)),
    getOff(baseRef, CFrame.new(599.1990966796875, 0.6625007390975952, 1194.0994873046875, -0.82660004323959351, 0.5627907514572144, -0.0023045071866363287, 0.000024882749130483717, -0.004058465827256441, -0.9999943375587463, -0.562795877456665, -0.8265911936759949, 0.003337712027132511)),
    getOff(baseRef, CFrame.new(603.1194458007812, 0.6291652917861938, 1188.3319091796875, -0.8266323208808899, 0.5627378821372986, -0.0022892996203154325, -0.00007098433707142249, -0.004172381944954395, -0.9999913573265076, -0.5627425909042358, -0.826624870300293, 0.003488971618935466)),
    getOff(baseRef, CFrame.new(605.01025390625, 0.6958326697349548, 1198.6744384765625, -0.8266060948371887, 0.5627815127372742, -0.0023060475941747427, 0.000008191634151444305, -0.004085747059434652, -0.9999938607215881, -0.5627865791320801, -0.8265970349311829, 0.003370110411196947)),
    getOff(baseRef, CFrame.new(602.8509521484375, 0.6178554892539978, 1193.3946533203125, 0.5612367987632751, 0.8276568055152893, -0.0007711328798905015, 0.00000668937582304352, -0.0009390025516040623, -0.9999995827674866, -0.8276553153991699, 0.5612375140190125, -0.0005331022548489273)),
    getOff(baseRef, CFrame.new(606.03564453125, 0.6074132919311523, 1203.3602294921875, 0.5612367987632751, 0.8276585936546326, -0.0007836095755919814, -0.000004503143827605527, -0.0009490738739259541, -0.9999995827674866, -0.8276553153991699, 0.5612384080886841, -0.00053001893684268)),
    getOff(baseRef, CFrame.new(610.3599243164062, 0.5334715247154236, 1206.084228515625, 0.5612367987632751, 0.8276591897010803, -0.0007669975166209042, 0.00000767213714425452, -0.000938120880164206, -0.9999995827674866, -0.8276553153991699, 0.561238706111908, -0.0005341245559975505)),
    getOff(baseRef, CFrame.new(605.60595703125, 0.5583335757255554, 1172.3504638671875, -0.8266000747680664, 0.5627914667129517, -0.002346049528568983, -0.000009411462997377384, -0.004185963422060013, -0.9999938607215881, -0.562795102596283, -0.8265914916992188, 0.003461291315034032))
}

-- [[ GUI SYSTEM ]] --
local sg = Instance.new("ScreenGui", CoreGui)
sg.Name = "Abyss_B2_Control"
local main = Instance.new("Frame", sg)
main.Size, main.Position = UDim2.new(0, 180, 0, 170), UDim2.new(0.05, 0, 0.4, 0)
main.BackgroundColor3, main.BorderSizePixel = Color3.fromRGB(15, 5, 25), 0
main.ClipsDescendants = true

local titleBar = Instance.new("Frame", main)
titleBar.Size, titleBar.BackgroundColor3 = UDim2.new(1, 0, 0, 25), COLORS.DarkAbyss
titleBar.BorderSizePixel = 0

local titleLabel = Instance.new("TextLabel", titleBar)
titleLabel.Size, titleLabel.Position = UDim2.new(1, -10, 1, 0), UDim2.new(0, 5, 0, 0)
titleLabel.BackgroundTransparency, titleLabel.Text = 1, "B-2 Spirit Stealth Bomber V"
titleLabel.TextColor3, titleLabel.Font, titleLabel.TextSize = Color3.new(1, 1, 1), Enum.Font.SourceSansBold, 12
titleLabel.TextXAlignment = Enum.TextXAlignment.Left

local creditLabel = Instance.new("TextLabel", main)
creditLabel.Size, creditLabel.Position = UDim2.new(1, 0, 0, 15), UDim2.new(0, 0, 1, -15)
creditLabel.BackgroundTransparency, creditLabel.Text = 1, "Engine: DYTON_txt"
creditLabel.TextColor3, creditLabel.Font, creditLabel.TextSize = COLORS.Accent, Enum.Font.Code, 10

-- Dragging System
local dragging, dragStart, startPos
titleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging, dragStart, startPos = true, input.Position, main.Position
    end
end)
UIS.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        main.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UIS.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then dragging = false end end)

local function styleBtn(b)
    b.BackgroundColor3, b.BorderSizePixel, b.BorderColor3 = COLORS.Void, 1, COLORS.DarkAbyss
    b.TextColor3, b.Font, b.TextSize = Color3.new(0.8, 0.8, 0.8), Enum.Font.SourceSansBold, 11
    b.MouseEnter:Connect(function() b.BorderColor3 = COLORS.Accent b.TextColor3 = Color3.new(1,1,1) end)
    b.MouseLeave:Connect(function() b.BorderColor3 = COLORS.DarkAbyss b.TextColor3 = Color3.new(0.8,0.8,0.8) end)
end

local container = Instance.new("Frame", main)
container.Size, container.Position, container.BackgroundTransparency = UDim2.new(1, -10, 1, -50), UDim2.new(0, 5, 0, 30), 1

local function createBtn(txt, pos, size)
    local b = Instance.new("TextButton", container)
    b.Text, b.Position, b.Size = txt, pos, size styleBtn(b)
    return b
end

local SummonBtn = createBtn("SUMMON B-2 SPIRIT", UDim2.new(0,0,0,0), UDim2.new(1,0,0.22,0))
local fBtn = createBtn("Fly: OFF", UDim2.new(0.5,2,0.26,0), UDim2.new(0.5,-2,0.22,0))
local upBtn = createBtn("UP", UDim2.new(0,0,0.26,0), UDim2.new(0.25,-2,0.22,0))
local dnBtn = createBtn("DN", UDim2.new(0.25,0,0.26,0), UDim2.new(0.25,-2,0.22,0))
local plusBtn = createBtn("+" , UDim2.new(0.5,2,0.52,0), UDim2.new(0.25,-2,0.22,0))
local minusBtn = createBtn("-", UDim2.new(0.75,2,0.52,0), UDim2.new(0.25,-2,0.22,0))
local sNum = createBtn("SPD: 1", UDim2.new(0,0,0.52,0), UDim2.new(0.5,-2,0.22,0))
local cBtn = createBtn("Noclip: OFF", UDim2.new(0,0,0.78,0), UDim2.new(1,0,0.22,0))

local function restoreNaturalCollision(char)
    if not char then return end
    for _, v in pairs(char:GetDescendants()) do
        if v:IsA("BasePart") then
            local name = v.Name:lower()
            v.CanCollide = (name:find("torso") or name:find("root") or name:find("leg") or name:find("head")) and not noclip
        end
    end
end

local function resetPhysics(char)
    if not char then return end
    local hum, root = char:FindFirstChild("Humanoid"), char:FindFirstChild("HumanoidRootPart")
    if hum then hum.PlatformStand = false hum:ChangeState(Enum.HumanoidStateType.Running) end
    if root then
        if root:FindFirstChild("CodeVel") then root.CodeVel:Destroy() end
        if root:FindFirstChild("CodeGyro") then root.CodeGyro:Destroy() end
        root.AssemblyLinearVelocity = Vector3.zero
    end
    restoreNaturalCollision(char)
end

local VisibleParts = {}
local function SetupCharacter()
    local char = LP.Character or LP.CharacterAdded:Wait()
    VisibleParts = {}
    for _, d in pairs(char:GetDescendants()) do
        if (d:IsA("BasePart") or d:IsA("Decal")) and d.Name ~= "HumanoidRootPart" then
            table.insert(VisibleParts, d)
        end
    end
end

local function ToggleInvis(state)
    IsInvisible = state
    local targetTrans = IsInvisible and 1 or 0
    for _, part in pairs(VisibleParts) do
        if part and part.Parent then pcall(function() part.Transparency = targetTrans end) end
    end
end

SummonBtn.MouseButton1Click:Connect(function()
    petMode = not petMode
    if petMode then
        SetupCharacter() ToggleInvis(true)
        flying, noclip = true, true
        fBtn.Text, cBtn.Text, SummonBtn.TextColor3 = "Fly: ON", "Noclip: ON", COLORS.Accent
        local p = workspace:WaitForChild("WorkspaceCom"):WaitForChild("001_TrafficCones")
        local targetName = (OwnerName == "terasiremik") and "Propterasiremik" or ("Prop" .. OwnerName)

        -- Ambil semua part mentah dari WorkspaceCom
        local rawProps = {}
        for _, v in ipairs(p:GetChildren()) do 
            if v.Name == targetName then table.insert(rawProps, v) end 
        end

        -- [[ ADVANCED JOSEP BART LOCK LOGIC ]] --
        -- 1. Bersihkan referensi prop yang sudah hancur/hilang dari database permanent lock
        for id, storedProp in pairs(PERMANENT_PROP_LOCK) do
            if not storedProp or not storedProp.Parent then
                PERMANENT_PROP_LOCK[id] = nil
            end
        end

        -- 2. Daftarkan prop baru ke slot ID kosong jika belum terdaftar sama sekali
        for _, prop in ipairs(rawProps) do
            local alreadyRegistered = false
            for id, storedProp in pairs(PERMANENT_PROP_LOCK) do
                if storedProp == prop then
                    alreadyRegistered = true
                    break
                end
            end
            
            if not alreadyRegistered then
                -- Cari slot ID 1-14 yang masih kosong untuk mengunci prop baru ini
                for id = 1, #B2SpiritParts do
                    if not PERMANENT_PROP_LOCK[id] then
                        PERMANENT_PROP_LOCK[id] = prop
                        break
                    end
                end
            end
        end

        -- 3. Bangun wingRemoteCache berdasarkan urutan ID database yang telah DIKUNCI MATI
        wingRemoteCache = {}
        for id = 1, #B2SpiritParts do
            if PERMANENT_PROP_LOCK[id] and PERMANENT_PROP_LOCK[id].Parent then
                table.insert(wingRemoteCache, PERMANENT_PROP_LOCK[id])
            end
        end
        
    else
        ToggleInvis(false) flying, noclip = false, false
        fBtn.Text, cBtn.Text, SummonBtn.TextColor3 = "Fly: OFF", "Noclip: OFF", Color3.new(0.8,0.8,0.8)
        resetPhysics(LP.Character) table.clear(currentCFrames)
        currentRoll = 0
        -- Catatan: PERMANENT_PROP_LOCK TIDAK DI-CLEAR agar memori index 1-14 tetap terkunci di objek yang sama!
    end
end)

fBtn.MouseButton1Click:Connect(function() flying = not flying fBtn.Text = flying and "Fly: ON" or "Fly: OFF" if not flying then resetPhysics(LP.Character) end end)
cBtn.MouseButton1Click:Connect(function() noclip = not noclip cBtn.Text = noclip and "Noclip: ON" or "Noclip: OFF" restoreNaturalCollision(LP.Character) end)
plusBtn.MouseButton1Click:Connect(function() flySpeed = flySpeed + 1 sNum.Text = "SPD: "..tostring(flySpeed) end)
minusBtn.MouseButton1Click:Connect(function() flySpeed = math.max(1, flySpeed - 1) sNum.Text = "SPD: "..tostring(flySpeed) end)
upBtn.MouseButton1Down:Connect(function() vVect = 1 end) upBtn.MouseButton1Up:Connect(function() vVect = 0 end)
dnBtn.MouseButton1Down:Connect(function() vVect = -1 end) dnBtn.MouseButton1Up:Connect(function() vVect = 0 end)

-- [[ MAIN FLIGHT INTERPOLATION ENGINE ]] --
RS.Heartbeat:Connect(function(dt)
    local char = LP.Character
    local root, hum = char and char:FindFirstChild("HumanoidRootPart"), char and char:FindFirstChild("Humanoid")
    local totalProps = #wingRemoteCache
    if not root or not hum or not petMode or totalProps < 1 then return end

    if IsInvisible then
        local oCF = root.CFrame
        local dCF = oCF * CFrame.new(0, -500000, 0)
        root.CFrame = dCF
        hum.CameraOffset = dCF:ToObjectSpace(CFrame.new(oCF.Position)).Position
        RS.RenderStepped:Wait()
        root.CFrame = oCF
        hum.CameraOffset = Vector3.zero
    end

    local t = tick()
    
    local currentPos = root.Position
    local calculatedVelocity = (currentPos - lastPosition) / math.max(dt, 0.001)
    lastPosition = currentPos

    local lagComp = flying and math.clamp(flySpeed * 0.25, 0, 50) or 0
    local safePing = math.clamp(estimatedPing, 0.01, 0.25)
    
    local predictedPosition = currentPos + (calculatedVelocity * safePing)
    local baseTargetCF = CFrame.new(predictedPosition, predictedPosition + root.CFrame.LookVector)
    
    local finalPos = baseTargetCF * CFrame.new(0, flying and -4 or 0, (flying and -5 or 0) - lagComp)
    
    local rotCF = CFrame.Angles(math.rad(MANUAL_ROTATION.X), math.rad(MANUAL_ROTATION.Y), math.rad(MANUAL_ROTATION.Z))
   local rollManeuverCF = CFrame.Angles(math.rad(currentRoll), 0, 0) 

    local lerpAlpha = 1 - math.exp(-14 * dt)

    for i, offset in ipairs(B2SpiritParts) do
        if i <= totalProps then
            local targetCF = finalPos * rotCF * rollManeuverCF * offset
            currentCFrames[i] = (currentCFrames[i] or targetCF):Lerp(targetCF, lerpAlpha)
        end
    end

    if t - lastNetworkUpdate > 0.014 then
        local canUpdateColor = (t - lastColorUpdate > 0.05)
        local frameCFrames = currentCFrames
        
        for i = 1, totalProps do
            local prop = wingRemoteCache[i]
            local targetCFrame = frameCFrames[i]
            
            if prop and targetCFrame then
                task.spawn(function()
                    local cfR = prop:FindFirstChild("SetCurrentCFrame")
                    if cfR then 
                        local startSend = os.clock()
                        pcall(function() 
                            cfR:InvokeServer(targetCFrame) 
                            if i == 1 then
                                estimatedPing = (estimatedPing * 0.85) + ((os.clock() - startSend) * 0.15)
                            end
                        end) 
                    end
                    
                    if canUpdateColor and i == colorIndex then
                        local crR = prop:FindFirstChild("ChangePropColor")
                        if crR then pcall(function() crR:InvokeServer(getAbyssColor(t, i)) end) end
                    end
                end)
            end
        end
        
        if canUpdateColor then
            colorIndex = (colorIndex % totalProps) + 1
            lastColorUpdate = t
        end
        lastNetworkUpdate = t
    end
end)

-- [[ RENDERING PHYSICAL MOVEMENT ]] --
RS.RenderStepped:Connect(function(dt)
    local char = LP.Character
    if flying and char and char:FindFirstChild("HumanoidRootPart") then
        local root, hum, cam = char.HumanoidRootPart, char.Humanoid, workspace.CurrentCamera
        local bv = root:FindFirstChild("CodeVel") or Instance.new("BodyVelocity", root)
        local bg = root:FindFirstChild("CodeGyro") or Instance.new("BodyGyro", root)
        bv.Name, bg.Name = "CodeVel", "CodeGyro"
        bv.MaxForce, bg.MaxTorque = Vector3.new(1e8, 1e8, 1e8), Vector3.new(1e8, 1e8, 1e8)
        bg.P, bg.D, hum.PlatformStand = 48000, 520, true
        
        local rel = cam.CFrame:VectorToObjectSpace(hum.MoveDirection)
        local rawVel = ((cam.CFrame.LookVector * -rel.Z) + (cam.CFrame.RightVector * rel.X) + (Vector3.new(0,1,0) * vVect))
        
        bv.Velocity = (rawVel.Magnitude > 0) and (rawVel.Unit * (flySpeed * 25)) or Vector3.zero
        bg.CFrame = cam.CFrame

        local targetRoll = 0
        local activeLerpSpeed = ROLL_SPEED

        if rawVel.Magnitude > 0 then
            if math.abs(rel.X) > 0.1 then
                targetRoll = math.clamp(-rel.X * MAX_ROLL, -MAX_ROLL, MAX_ROLL)
            else
                targetRoll = 0
            end
        else
            local rawMouseDelta = UIS:GetMouseDelta()
            smoothedMouseX = smoothedMouseX + (rawMouseDelta.X - smoothedMouseX) * math.min(18 * dt, 1)
            
            if math.abs(smoothedMouseX) > 0.5 then
                targetRoll = math.clamp(-smoothedMouseX * 1.8, -MAX_ROLL, MAX_ROLL)
            else
                targetRoll = 0
            end
        end
        
        if math.abs(targetRoll) < 0.5 then
            targetRoll = 0
            activeLerpSpeed = BASE_RETURN_SPEED + (math.abs(currentRoll) * 0.4) 
        end
        
        currentRoll = currentRoll + (targetRoll - currentRoll) * (1 - math.exp(-activeLerpSpeed * dt))
    else
        currentRoll = currentRoll + (0 - currentRoll) * (1 - math.exp(-22 * dt))
    end
    
    if noclip and char then 
        for _, v in pairs(char:GetDescendants()) do if v:IsA("BasePart") then v.CanCollide = false end end 
    end
end)

UIS.InputBegan:Connect(function(input, g) if not g and input.KeyCode == Enum.KeyCode.G then ToggleInvis(not IsInvisible) end end)
local conn = LP.CharacterAdded:Connect(function() task.wait(0.5) SetupCharacter() end)
SetupCharacter()
